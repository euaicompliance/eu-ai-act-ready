<?php
/**
 * PHPUnit bootstrap.
 *
 * These are plain unit tests: they load only WordPress-independent classes, so no
 * WordPress installation or test suite is required.
 *
 * @package EUAIACTREADY
 */

define( 'ABSPATH', __DIR__ . '/' );

require_once dirname( __DIR__ ) . '/includes/class-euaiactready-media-markup.php';
